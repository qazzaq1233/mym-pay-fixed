# MYM码支付系统 v2.7.0 修复优化版

## 修复说明

本版本基于 MYM 开源码支付系统 v2.7.0 进行了优化和修复，提升了兼容性、安全性和稳定性。

---

## 修复内容

### 一、PHP 8.x 兼容性优化
1. **Session 启动优化**：使用 `session_status()` 检测会话状态，避免 PHP 8 下重复启动 session 的警告
2. **错误处理优化**：增加调试模式开关，生产环境隐藏错误，开发环境可开启调试
3. **移除重复代码**：清理重复的时区设置（Asia/Shanghai 和 PRC 重复设置）
4. **Session 安全配置**：增加 `cookie_httponly` 和 `use_only_cookies` 安全配置

### 二、安全增强
1. **安全响应头**：增加 X-Content-Type-Options、X-Frame-Options、X-XSS-Protection 安全头
2. **数据库错误信息隐藏**：生产环境不暴露详细数据库错误信息
3. **清理默认配置**：移除原作者的默认数据库账号密码，改为通用示例配置
4. **Session 安全加固**：启用 HttpOnly Cookie，防止 XSS 窃取会话

### 三、代码优化
1. **代码格式统一**：统一缩进和代码风格
2. **增加版本常量**：定义 `MYM_OPEN_SOURCE` 和 `MYM_VERSION` 常量
3. **调试模式**：新增 `MYM_DEBUG` 常量，方便调试和排错
4. **默认二维码 API 降级**：当默认的 QQ 二维码 API 不可用时，自动降级到 qrserver.com

### 四、配置优化
1. **干净的配置文件**：Config.php 使用通用默认值，避免误导
2. **移除安装锁**：install.lock 已删除，可直接运行安装程序
3. **清理备份文件**：移除无用的配置备份文件

---

## 系统要求

- **PHP 版本**：7.1 ~ 8.2（推荐 PHP 7.4 或 8.1）
- **MySQL 版本**：5.6 及以上（推荐 5.7 或 8.0）
- **Web 服务器**：Apache / Nginx
- **PHP 扩展**：PDO、pdo_mysql、curl、gd、mbstring

---

## 安装步骤

### 1. 上传文件
将所有文件上传到网站根目录

### 2. 设置权限
确保以下目录/文件有写入权限：
- `Mym/Config.php`
- `Mym/cache/` 目录
- `install/` 目录（安装完成后可删除）

### 3. 运行安装程序
访问 `http://你的域名/install/` 按照提示完成安装

### 4. 安装后安全设置（重要）
1. 删除 `install/` 目录或重命名
2. 修改后台默认密码
3. 修改 `cronkey` 定时任务密钥
4. 开启 HTTPS（推荐）

---

## 调试模式

如需开启调试模式查看错误信息，请编辑 `Mym/Common.php`：

```php
// 将 false 改为 true
define('MYM_DEBUG', true);
```

**注意**：生产环境请务必关闭调试模式！

---

## 常见问题

### Q: 安装后显示空白页
A: 开启调试模式（MYM_DEBUG=true）查看具体错误信息，通常是数据库配置错误或PHP扩展缺失

### Q: 二维码不显示
A: 在后台切换二维码生成 API，推荐使用 api.qrserver.com

### Q: 支付回调不生效
A: 检查云端接口是否可用，确认服务器可以访问外部网络

### Q: PHP 8 下报错
A: 本版本已优化 PHP 8 兼容性，如仍有问题请开启调试模式查看具体错误

---

## 安全建议

1. **定期备份数据库**
2. **修改默认管理员密码**
3. **不要使用弱密码**
4. **开启 HTTPS**
5. **安装完成后删除 install 目录**
6. **定期检查更新**
7. **修改 cronkey 为随机字符串**

---

## 免责声明

本程序为开源码支付系统，仅供学习研究使用。使用本程序进行经营活动请遵守相关法律法规，使用者自行承担使用风险。

---

**修复版本**：v2.7.0-fixed
**更新日期**：2026-07-26
