# 2026-06-19

- 修复并重构 `install/index.php` 安装向导：移除依赖不存在的 `../Mym/Static/...` 静态资源和 `jquery.bootstrap.wizard.min.js`，改用项目实际存在的 `Mym/Assets/Login` 资源；重写步骤展示逻辑，补充数据库配置转义、端口校验、PDO 错误提示区分、安装锁页面，并将安装页美化为响应式玻璃拟态风格。
- 分析线上 `mym.8b2.com` 前台与 `/Admin/` 同时 HTTP 500：入口都先加载 `Mym/Common.php`，本地项目缺少 `Mym/Config.php`，线上若安装后仍 500，优先查 `Mym/Config.php`、PDO/mysql 扩展、数据库连接、`pay_config` 表及 `DB_VERSION`/`VERSIONS` 常量定义；PHP 7.4 符合项目入口要求，推荐 PHP 7.4。
- 查看宝塔错误日志截图：当前日志主要是安装页旧版引用 `/Mym/Static/css/bootstrap.min.css`、`materialdesignicons.min.css`、`style.min.css`、`jquery.min.js`、`jquery.bootstrap.wizard.min.js`、`bootstrap.min.js` 等不存在资源，并因缺少 `/404.html` 记录 open failed；这说明线上仍是旧安装页或未覆盖新版 `install/index.php`，该日志不是前后台 500 的 PHP Fatal 明细。
- 安装成功后继续排查前后台 500：本地开源源码缺少 `Mym/authcode.php`，而 `Mym/Common.php` 第 59 行 `require_once SYSTEM_ROOT."authcode.php"` 必须加载它；同时 `DB_VERSION`、`VERSIONS` 常量仅被使用未被定义。已新增 `Mym/authcode.php` 定义 `DB_VERSION=1410`、`VERSIONS='1.4.10'`，需上传到线上 `/www/wwwroot/mym.8b2.com/Mym/authcode.php` 后再测前后台。
- 针对前后台出现“你应使用的版本过于老旧”阻断：该提示由原加密版远程授权/版本检查逻辑返回，开源版应去除。已更新 `Mym/authcode.php` 为本地开源兼容文件，补充 `VERSION`、`VERSIONS`、`$authcode`；并修改 `Mym/Mym_Api/Mym.Api.php` 中 `Update()`、`AuthUpdate()`，不再请求 `auth.iizi.cn/check.php`，直接返回本地开源版通过状态。
