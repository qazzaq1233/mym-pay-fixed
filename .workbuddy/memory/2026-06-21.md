# 2026-06-21

- 继续 Go 版本系统迁移：在 `go-mym/` 中实现 `/submit` 与 `/mapi` 基础下单接口，兼容现有 PHP 的 `pid/type/out_trade_no/notify_url/return_url/name/money/sign/sign_type` 参数，新增 `model.SubmitRequest/SubmitResult/InsertOrderInput`、`service.SubmitOrder()`、`repository.SelectAvailableChannel()/HasActivePrice()/InsertOrder()/MarkChannelUsed()`，支持商户 MD5 签名校验、可用通道选择、金额递增避重、订单写入 `pay_order`、submit 跳转 `/pay`、mapi 返回 JSON。完善 `/pay` 支付页读取订单/通道、二维码内容和收款人姓，新增 `/order/status` 订单状态接口及支付页轮询/倒计时/成功跳转；用户中心接入基础统计（总用户、总订单、总通道、今日成功订单、今日金额）。已更新 `go-mym/README.md` 说明当前入口与后续迁移重点，并做 Go 源文件括号/引号结构检查；当前环境无 Go 命令，未做 `go test`/`go build`。
- 继续补齐 Go 版支付闭环：增强 `go-mym` 支付页，生成二维码图片地址、支付方式中文名、通道代码、H5 唤醒链接和收款人姓 Unicode 兼容恢复；新增 `service/notify.go`，实现订单完成、商户回调签名/URL 生成、可选通知请求、扣减商户余额、写入 `pay_notify`/`pay_log`；新增 `/notify`、`/order/complete`、`/api/order` 逻辑与路由，支持回调触发和商户签名查单。因当前环境无 Go 工具链，只做了关键 Go 文件括号结构与引用点检查。
- 继续扩展 Go 版迁移：新增 `/qqh5` 路由，兼容 PHP `Submit/QQH5.php` 的 QQ 钱包 H5/SDK 唤醒；新增 `service/channel_config.go` 和 `/api/pay-channel-config`，读取并合并 `pay_config.pay_channel_config` 与默认支付方式/通道配置；新增后台基础 API `/admin/api/users`、`/admin/api/orders`、`/admin/api/channels`，通过 `X-Admin-Secret` 或 `admin_secret` 查询参数鉴权并返回分页数据。再次做 Go 文件括号结构检查；仍因环境缺少 Go 工具链未执行编译。
- 按用户要求将 `go-mym` 安装流程调整为类似 XArrPay 宝塔 Go 项目安装：默认端口改为 `32010`（`config.example.json` 与配置默认值同步）；新增 `/install` 安装向导页面，包含协议、环境检查、数据库/站点/管理员配置、自动导入 `install/install.sql`、写入 `configs/config.json`；未安装或数据库不可用时仍可启动并访问安装页，业务路由在未安装时跳转 `/install`。更新 `README.md` 为宝塔安装流程说明，并复制 PHP 原 `install/install.sql` 到 `go-mym/install/install.sql`。
