# 2026-06-20

- 重构 MYM 码支付开源版访问兼容链路：`Mym/authcode.php` 改为本地开源兼容入口，定义 `MYM_OPEN_SOURCE`、`DB_VERSION=1410`、`VERSION/VERSIONS=1.4.10-open`、`$authcode` 与本地通过状态；`Mym/Common.php` 在开源模式下不再强制旧版升级拦截，并补齐 `KEY`、验证码、模板、CDN、二维码等缺省配置；`Mym/Mym_Api/Mym.Api.php` 禁用旧远程授权/版本/扫码登录服务，`PayName()` 本地放行；`Admin/page/workplace.php` 去除旧授权入口展示；`Admin/page/updata.php` 禁用旧授权服务器自动更新；`install/update.php` 删除缓存前增加文件存在判断；`index.php` 修复 `mod` 参数解析错误。目标是安装成功后恢复前台与后台正常访问，避免“版本过于老旧”远程提示拦截。
- 继续排查“版本过于老旧”提示真实来源：在 `Mym/Mym_Class/Pay_function.php` 文件末尾发现硬编码到期拦截 `if(time()>1735653011){ sysmsg(urldecode(...), true); }`，其中 URL 编码内容即“温馨提示：你应使用的版本过于老旧，请更新最新版本，若不更新将无法使用”。已将该时间锁重构为开源版禁用逻辑，上传覆盖 `Mym/Mym_Class/Pay_function.php` 后应恢复前后台访问。
- 优化用户认证页视觉 ID：`User/Login.php`、`User/Reg.php`、`User/findpwd.php`、`User/Connect.php` 增加统一 `auth-page/auth-hero` 大厂风格结构与文案；重写 `Mym/Assets/Login/static/css/main.css`，改为浅色玻璃拟态、蓝色科技渐变、现代卡片输入框、主/次按钮、QQ 快捷登录卡片和响应式布局。已用脚本检查页面 div/form 标签闭合与关键 CSS 标记；当前环境无 `php` 命令，未能做 PHP lint。
- 修复 `User/Login.php` 点击登录无响应：控制台报 `GeetestError: initGeetest 里面的 gt 或 challenge 参数缺少`，原因是 `Ajax.php?act=Captcha` 返回空极验参数导致 `initGeetest` 异常，原点击事件又依赖验证码初始化后才绑定。现将登录提交封装为 `submitLogin()`，提前绑定 `#login` 点击事件；验证码参数缺失或初始化失败时自动降级为普通账号密码登录，保留极验可用时的验证流程。
- 支持支付宝通道手填收款码链接：`User/Free_Qrlist.php` 在支付宝 `mg_ali` 时显示“支付宝收款码链接”输入与提示；`User/User_Js/channel.js` 增加前端必填和链接协议校验；`User/Ajax.php` 后端校验 `mg_ali` 必须填链接并保存为启用状态；`Mym/Mym_Class/Pay_function.php` 的 `qrdecode()` 优先使用 `mg_ali` 手填 `qr_url`，普通监控/补单遇到手填且无 cookie 时跳过 cookie 检测；`install/install.sql` 与 `install/1411.sql` 将 `pay_qrlist.qr_url` 扩到 varchar(1000)。
- 精简支付宝免 CK/云端通道配置：`User/User_Js/channel.js` 中支付宝 `hook==2` 更新弹窗不再展示“扫码登录/账号密码登录/更新原有应用/创建新的应用”，直接进入自定义配置；`User/Free_Qrlist.php` 自定义配置只保留应用 AppID、应用私钥，并新增支付宝收款码链接，移除支付宝公钥输入；`User/Ajax3.php` 的 `AliYunGet` 同时返回 `pay_qrlist.qr_url`；`User/Ajax2.php?act=user_settle_save` 改为校验并保存 AppID、私钥与收款码链接到 `pay_alidata`/`pay_qrlist.qr_url`；`Mym/Mym_Class/Pay_function.php` 对支付宝免 CK手动链接模式跳过 COOKIE 检测。
- 修复 MYM 支付宝免 CK/云端通道无法监控余额和自动回调：参考 `别的系统免ck通道/alipay_bill/plugin.lua` 的商家账单逻辑，移除 `yunck_money_notify()` 中 `alipay + hook_type=2 + qr_url` 直接 return 的查账拦截；改为先拉取 `AlipayDataBillAccountlogQueryRequest()` 账单，过滤收入/转账/交易，清理备注前缀 `请勿添加备注-`，优先按 `trade_no/out_trade_no == trans_memo + 金额` 匹配未支付订单，失败再按金额兜底匹配并触发现有 `creat_callback()`/`do_notify()`/`pay_notify()`；余额查询保留为兜底并更新 `pay_qrlist.money`。同时在支付宝余额/账单接口中允许 `appkey` 为空时跳过响应验签，并捕获 SDK 异常返回 JSON 错误，避免无支付宝公钥时直接中断。
- 继续排查支付宝免 CK 仍不回调：判断官方接口请求本身不强制填写支付宝公钥，私钥+AppID可签名请求；公钥仅用于本地验签。进一步修复 `Mym/Mym_Class/Alipay_Class.php` 私钥兼容，支持 PKCS#1 与 PKCS#8/带 PEM 头尾格式，避免只按 `BEGIN RSA PRIVATE KEY` 包装导致新版支付宝应用私钥签名失败；验签公钥无效时不再 `die`。`yunck_money_notify()` 账单匹配新增 `pay_type=ALIPAYACCOUNT` 过滤，并支持备注匹配 `out_trade_no`。
- 检查用户填写的支付宝应用私钥格式：用户直接粘贴的是无 PEM 头尾的长 Base64 私钥，截图里为单行填入；判断格式本身可被修复后的 SDK 兼容，但已暴露在对话中，建议尽快在支付宝开放平台重新生成/更换应用私钥并更新系统配置。未记录具体密钥内容。
- 按截图需求增加支付宝免 CK/云端“订单检查方式”整体配置：`User/Free_Qrlist.php` 自定义配置弹窗新增按钮式选择“订单号匹配不到则使用金额 / 订单号”；`User/User_Js/channel.js` 新增选中状态切换、读取回显和保存提交；`User/Ajax2.php` 保存 `ali_order_check` 到 `pay_qrlist.json`；`User/Ajax3.php` 读取回显该配置；`User/Ajax.php` 新增通道时默认写入该配置；`Mym/Mym_Class/Pay_function.php::yunck_money_notify()` 按配置执行，默认优先订单号匹配，失败后允许按金额兜底回调，选择“订单号”时禁用金额兜底。已做基础括号结构检查通过。
- 补全支付宝免 CK/云端余额显示：确认通道列表余额来自 `pay_qrlist.money`，定时任务 `yunck_money_notify()` 已会通过 `AlipayDataBillBalanceQueryRequest()` 读取 `available_amount` 并更新余额；新增保存自定义配置时立即用 AppID+应用私钥查询支付宝余额，成功则同步写入 `pay_qrlist.money`，失败则在保存提示中返回余额获取失败原因。`User/Free_Qrlist.php` 配置弹窗新增只读“当前账户余额”，`User/Ajax3.php` 回传 money，`User/User_Js/channel.js` 回显余额。已做基础括号结构检查通过。
- 修复支付宝免 CK/云端金额兜底导致历史 0.01 收款重复回调新订单的问题：`Mym/Mym_Class/Pay_function.php::yunck_money_notify()` 账单匹配时提取 `alipay_order_no/merchant_order_no` 作为第三方流水，已成功订单若存在相同 `api_trade_no` 则跳过；回调前把该流水写入 `pay_order.api_trade_no`，避免同一支付宝账单重复使用。金额兜底新增交易时间约束，账单 `trans_dt` 必须落在订单 `addtime <= trans_dt <= outtime` 范围内；余额查询只更新 `pay_qrlist.money` 显示，不再用余额差额直接回调订单，避免私下转账或历史余额差额误判未付款订单成功。已做基础括号结构检查通过。
- 完成 QQ 云端后台适配：参考 `别的系统qq云端通道对接方式/yun_qqpay_lz/plugin.lua` 提取 `QQ_Pc/CreateID`、`QQ_Pc/QRCode`、`QQ_Pc/IsLoginStatus` 协议；`Mym/Mym_Api/Mym.Qq.Api.php` 新增 QQ_Pc 请求封装并兼容旧 `/Api/Add_QQ`、`/Api/Add_Qr`；`User/Ajax.php` 创建 QQ 云端登录二维码时传站点、商户 PID、商户 KEY，登录成功保存 uid 作为本地 cookie；`Admin/Ajax.php` 规范云端地址为 http(s) 根地址并自动补 `/`；`Admin/page/yund.php` 后台类型默认突出 QQ云端-QQ_Pc，提示示例 `https://xdyd.9o3.cn/`；`User/Free_Qrlist.php` 修正微信类型误包含 `yd_qq`；`Mym/Mym_Class/Pay_function.php` 对 `yd_qq` 的 uid 型 cookie 使用 `IsLoginStatus` 检测在线，避免当成 QQ cookie 解析。已用 Python 做修改文件基础括号结构检查通过；当前环境无 php 命令，未做 `php -l`。
- 继续修复 QQ 云端“获取登录二维码失败”：排查确认 `https://xdyd.9o3.cn/QQ_Pc/CreateID`、`QRCode`、`IsLoginStatus` 直接访问返回 404。已增强 `Mym/Mym_Api/Mym.Qq.Api.php`：QQ_Pc 请求支持 `/QQ_Pc/`、`/index.php/QQ_Pc/`、`/api/QQ_Pc/`、`/Api/QQ_Pc/` 候选路径，返回 HTTP 状态、请求 URL 和更明确错误；二维码字段兼容 `url/qr_url/qrcode/qrCode/qr/data`，支持 base64 转 `data:image/png;base64`。`User/Ajax.php` 对通道、登录服务器、云端状态增加校验并避免空云端导致 PHP notice；`User/User_Js/channel.js` 改为仅 `code==1` 且二维码地址合法时显示 `<img>`，否则显示明确错误并避免破图。已做基础括号结构检查通过。
- 修复 QQ钱包免挂/云端扫码后秒显示“CK状态失效”：`Mym/Mym_Class/Pay_function.php::Pay_Money_Get()` 修正 QQ cookie 解析，补全 `p_skey/skey/qluin/uin` cookie 头并放宽 `retmsg` 判断；`check_money_notify()` 对 `mg_qq` 余额接口短暂失败不再立即置 `status=0`，只更新 `crontime` 并提示建议重新扫码/改用云端；`yunck_money_notify()` 对 `yd_qq` 余额接口失败但云端在线路径不再误判掉线。原因判断为 QQ/财付通余额检测接口兼容性变差，不能再把单次余额接口失败等同 CK 失效。当前环境无 PHP 命令，未做 `php -l`。
- 修复 QQ钱包支付页继续展示固定普通收款码导致“当前二维码无法收款”：`Mym/Mym_Class/Pay_function.php::qrdecode()` 的 `qqpay` 分支改为必须按订单金额生成动态金额码；旧 QQ 钱包 cookie 路径调用 `Add_Yun_Qrcode()` 生成 `qianbao.qq.com/wallet/sqrcode` 金额码，`yd_qq` uid 云端路径新增尝试调用 QQ_Pc 云端金额码候选接口（SetMoney/SetAmount/PayQrcode 等），失败时返回明确错误而不再静默回退 `pay_qrlist.qr_url`。`submit.php`/`mapi.php` 对 QQ 金额码失败直接提示错误，非 QQ 通道才保留固定码回退。`Mym/Mym_Api/Mym.Qq.Api.php` 修正 `Add_Yun_Qrcode()` cookie 解析与金额分单位计算，新增 QQ_Pc 金额码提取方法，并修复 `qrcode_ad()` 硬编码 `payee_uin=524430375`、未定义 `$skey` 和失败直接 exit 的问题。已用 Python 做冲突标记和大括号平衡检查；当前环境无 PHP 命令，未做 `php -l`。
- 按用户截图确认 QQ钱包“设置金额”适用免挂收款：QQ 端支持在“二维码收款”里设置金额（截图显示金额输入、最高 30000 元、收款说明可选），抓包看到 `wup.imtt.qq.com:8080` 与 `mqq.tenpay.com` 请求，符合本地 `qluin/p_skey` cookie 调用 `qr_code_generate.cgi` 生成金额码的条件。根据用户要求“云端那个靓仔不用管了”，已收敛修改：`qrdecode()` 只对 `qqpay + hook_type=0 + channel=mg_qq` 调用 `Add_Yun_Qrcode()` 生成动态金额码；其他 QQ 通道（含 `yd_qq` 靓仔云端）保留原固定码逻辑；`Mym.Qq.Api.php` 移除上一版 QQ_Pc 云端金额码候选方法，`submit.php`/`mapi.php` 仅对 `mg_qq` 禁止空码回退。已复查无冲突标记/异常字符且大括号平衡；当前环境无 PHP 命令，未做 `php -l`。
- 新增通用“自定义收款码链接”通道能力：`User/Free_Qrlist.php` 在添加通道弹窗加入自定义收款码链接输入，并针对支付宝/微信/QQ免挂收款码显示不同提示；`User/User_Js/channel.js` 提交 `custom_qr_url` 并校验 http(s)、alipays、alipayqr、weixin、mqqapi 等协议；`User/Ajax.php` 保存自定义链接到 `pay_qrlist.qr_url`，同时在 `json` 写入 `custom_qr_url=1` 标记并将通道置为启用；`Mym/Mym_Class/Pay_function.php::qrdecode()` 遇到该标记直接返回自定义链接；`submit.php` 与 `mapi.php` 下单时优先选择自定义收款码通道，因此配置了链接后发起订单会统一走该收款码收款。已做 JS 语法检查和 PHP 大括号平衡检查；当前环境无 php 命令，未做 `php -l`。
- 按用户补充要求修正“自定义收款码链接”语义：添加通道时允许填写收款码链接但仍保持原有扫码登录 CK/在线流程，不再因填写链接就跳过登录或直接启用。`User/Ajax.php` 改为把自定义链接保存到 `json.custom_qr_url`，`qr_url/cookie/status` 仍走原添加逻辑；`Mym/Mym_Class/Pay_function.php::qrdecode()` 仅在发起支付出码时返回 `json.custom_qr_url`，从而避免系统自动生成金额码；`submit.php`/`mapi.php` 优先选择在线且配置了自定义链接的通道，QQ 仍要求 `crontime` 在线；`User/Free_Qrlist.php` 提示文案改为“仍需扫码登录 CK，并保持在线”。已通过 JS 语法检查和 PHP 大括号平衡检查；当前环境无 php 命令，未做 `php -l`。
- 按截图需求完成支付通道展示配置：新增 `Mym/Mym_Class/Display.php` 通道默认配置/读取/保存/启用判断函数，`pay_type()` 改为优先使用后台自定义名称；`User/Free_Qrlist.php` 添加通道弹窗的大通道和子通道下拉改为读取后台配置，关闭的通道不展示，并修正默认类型切换逻辑；`User/Ajax.php` 添加通道时校验后台是否启用；后台新增 `Admin/page/pay_channel.php`，在 `Admin/index.php` 更多配置中加入“通道展示配置”，支持像云端管理一样编辑名称、排序和开关启用/关闭；`Admin/Ajax.php` 新增 `pay_channel_config` 与 `save_pay_channel_config` 接口。已做关键标记与大括号平衡检查；当前环境无 PHP 命令，未做 `php -l`。
- 根据用户反馈修正后台“通道展示配置”：`Admin/Ajax.php::save_pay_channel_config` 改为只更新提交字段，开关启用/关闭不再要求 `name`，解决直接点击开关时报“名称不能为空”；`pay_channel_config` 支持按 `type` 过滤子通道。`Admin/page/pay_channel.php` 文案从“大通道/子通道”调整为“支付方式/通道”，页面改为先展示支付宝、微信、QQ钱包、USDT 等支付方式，点击支付方式后下方只显示该支付方式所属通道，符合“支付方式 -> 通道”的分类关系。已做关键标记与大括号平衡检查；当前环境无 PHP 命令，未做 `php -l`。
- 增加后台自由新增支付方式和通道功能：`Mym/Mym_Class/Display.php::mym_pay_channel_config()` 不再删除非默认配置，允许保留自定义支付方式/通道，并校验通道所属支付方式存在；`Admin/Ajax.php::save_pay_channel_config` 增加 `is_new`、代码格式校验、重复代码校验、通道所属支付方式保存。`Admin/page/pay_channel.php` 增加“新增支付方式”“新增通道”按钮，新增时可填写代码、名称、所属支付方式、排序、启用状态。`User/Free_Qrlist.php` 和 `User/User_Js/channel.js` 改为统一使用动态 `#channel` 下拉，所有自定义支付方式都会按后台配置显示其下属通道并提交对应代码。已做关键标记与大括号平衡检查；当前环境无 PHP 命令，未做 `php -l`。
- 完成用户端单个支付通道测试功能：`User/Free_Qrlist.php` 在每条已添加通道记录操作区新增“测试通道”按钮；`User/User_Js/channel.js` 新增 `Test_Qr()`/`Submit_Test_Qr()` 弹窗，填写测试金额后调用 `Ajax.php?act=Test_Qr_Order` 并跳转 `/Submit/Mym_Pay.php?trade_no=...`；`User/Ajax.php` 新增 `Test_Qr_Order`，校验通道归属、启用/在线状态、后台通道配置状态，只使用当前 `pay_qrlist.id` 生成测试订单并写入 `qr_id`；`User/SDK/notify_url.php` 与 `User/SDK/return_url.php` 对“测试商品”按回调 `pid` 读取当前商户 key 验签，便于测试回调。已做关键标记与括号/引号平衡检查；当前环境无 PHP 命令，未做 `php -l`。
- 修复 QQ 通道解码收款码链接在支付页破图：`Submit/Mym_Pay.php` 原来直接把 `pay_order.qr_url` 拼到 `$qrcodeapi` 后面，QQ/微信等链接包含 `&`、`?` 等参数时未 URL 编码，导致二维码生成接口参数被截断，页面显示破图。现统一 `urldecode()` 取真实收款码内容后用 `urlencode()` 传给二维码接口；本地/远程 `.png` 图片链接保留直接显示逻辑，USDT 分支也补充 URL 编码。已做关键标记与括号/引号平衡检查；当前环境无 PHP 命令，未做 `php -l`。
- 新增官方简洁风格支付模板 `Submit/Template/official/index.php`，复制默认模板 assets 资源到 `Submit/Template/official/assets`，后台支付模板下拉可自动识别 `official`。新模板保留 default 模板全部 PHP/JS 变量和支付轮询逻辑（`$typeName/$type/$srow/$price/$QR_row/$userrow/$trade_no/$mp3/$cdnpublic/$outtime/$qr_url/$payh5url/$h5api`、`Mym_Get.php`、`QQH5.php`、倒计时、H5 唤醒、支付成功跳转等），UI 改为白底卡片、顶部官方收银台、安全状态、订单信息、二维码支付区和右侧扫码指引。已对比新旧模板变量无缺失并做括号/引号平衡检查；当前环境无 PHP 命令，未做 `php -l`。
- 增加每个支付通道单独设置“收款人姓”功能：`User/Free_Qrlist.php` 添加新增通道时的“收款人姓”输入、通道列表显示“姓：未设置/姓：X”，并给每条通道新增“设置姓”按钮；`User/User_Js/channel.js` 新增 `Set_Receiver_Surname()`/`Submit_Receiver_Surname()` 弹窗与保存接口调用，添加通道时同步提交该字段；`User/Ajax.php` 将姓保存到 `pay_qrlist.json.receiver_surname`，新增 `Set_Receiver_Surname` 接口，留空会删除该配置；`Submit/Mym_Pay.php` 统一读取 `$receiver_surname/$receiver_surname_html`，并在 `Submit/Template/default`、`111111`、`official` 以及 `Submit/Pay/Usdt.php`、`Skd.php`、`VzqPay.php`、`alipayfk.php`、`dmf.php`、`tz.php` 中按设置显示“姓：X”，未设置则不显示。已做关键标记覆盖检查；当前环境无 PHP 命令，未做 `php -l`。
- 修复收款人姓中文显示为 `u4f60`/Unicode 转义乱码的问题：`Mym/Mym_Class/Pay_function.php::jsondet()` 改为 `json_encode(..., JSON_UNESCAPED_UNICODE)`，后续保存中文姓氏不再转义；`Mym/Mym_Class/Function.php` 新增 `mym_restore_unicode_text()`，兼容读取历史已保存的 `uXXXX` 或 `\\uXXXX` 转义内容；`User/Free_Qrlist.php` 和 `Submit/Mym_Pay.php` 在列表、设置弹窗回显和支付页展示前统一还原中文。已做关键文件括号/引号结构检查；当前环境无 PHP 命令，未做 `php -l`。
- 按参考站用户中心风格改造用户端整体 UI：`User/Head.php` 引入新增样式 `User/Assets/assets/css/mym-user-modern.css`，重做左侧菜单分组、Logo、顶部栏、余额提示和用户下拉，保留所有原链接/变量/登录判断；新增 CSS 覆盖侧栏、顶部栏、卡片、表格、表单、按钮、徽章、套餐卡和响应式布局，整体改为白底浅灰背景、蓝色官方风格、圆角卡片与轻阴影。`User/index.php` 增加官方化欢迎横幅和快捷操作区，仍保留 `Ajax3.php?act=getcount` 等统计变量；`User/Free_Qrlist.php` 增加现代工具栏，原新增通道、测试通道、设置姓、开关/删除等功能不变。已做关键标记与括号/引号结构检查；当前环境无 PHP 命令，未做 `php -l`。
- 新建 Go 版本系统骨架 `go-mym/`，与现有 PHP 系统并行不替换：包含 `go.mod`、`cmd/server/main.go`、`configs/config.example.json`、配置加载、MySQL 连接、基础 HTTP 路由、JSON 响应、签名工具、用户/订单/通道/配置模型、Repository/Service/Handler 分层，以及首页、用户中心、后台、支付页模板占位。核心模型兼容现有 `pay_user`、`pay_order`、`pay_qrlist`、`pay_config` 表；基础路由包含 `/healthz`、`/user`、`/admin`、`/pay`、`/submit`、`/mapi`、`/notify`。已做关键文件与 Go 源文件括号/引号结构检查；当前环境未安装 Go 命令，未做 `go test`/`go build`。
